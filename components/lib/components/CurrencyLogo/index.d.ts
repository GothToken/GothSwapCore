import { Currency } from '@pangolindex/sdk';
import React from 'react';
export default function CurrencyLogo({ currency, size, style, }: {
    currency?: Currency;
    size?: string;
    style?: React.CSSProperties;
}): JSX.Element;
